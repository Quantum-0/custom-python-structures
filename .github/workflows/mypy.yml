name: MyPy

on: [pull_request]

jobs:
  black-list:
    name: MyPy
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: jpetrucciani/mypy-check@master
      with:
        path: 'src/*'